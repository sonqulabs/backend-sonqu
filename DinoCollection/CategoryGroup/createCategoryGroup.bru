meta {
  name: createCategoryGroup
  type: http
  seq: 2
}

post {
  url: {{BACKEND_URL}}/category-group
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

body:json {
  {
    "name":"Sabor"
  }
}
