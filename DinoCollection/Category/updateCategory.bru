meta {
  name: updateCategory
  type: http
  seq: 4
}

patch {
  url: {{BACKEND_URL}}/category/9
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

body:json {
  {
    "name":"principalrrre"
  }
}
